const busylight = require('busylight').get();

function controlBusylight(communicationActivities) {
  
    // Control Kuando Busylight based on communication activities
  }
  
  module.exports = {
    controlBusylight
  };
  
  function tryBusylight(){
 
    busylight.light('orange')
  }

  tryBusylight()